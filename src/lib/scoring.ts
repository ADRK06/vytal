// TODO: single source of truth for posture + hydration scoring math.
// Hydration scoring will get recalibrated as real calibration data comes in.

export function scorePosture(): number {
  return 0;
}

export function scoreHydration(): number {
  return 0;
}
