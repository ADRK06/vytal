// TODO: single session detail view — posture/hydration history + correlation chart.
export default function SessionDetailPage({
  params,
}: {
  params: { id: string };
}) {
  return <main>{/* session {params.id} detail */}</main>;
}
