NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — never expose to the browser. Used by POST /api/ingest to
# write hydration readings on the ESP32 mouse's behalf, bypassing Row Level
# Security (the device has no Supabase Auth session of its own).
SUPABASE_SERVICE_ROLE_KEY=
